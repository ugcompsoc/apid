name: V2 Base Route

testcases:
- name: GET V2 Root
  steps:
  - type: http
    method: GET
    url: "{{.url}}/v2"
    timeout: 5
    assertions:
    - result.statuscode ShouldEqual 200
    - result.bodyjson ShouldContainKey "data"
    - result.bodyjson.data ShouldContain "Root V2"
